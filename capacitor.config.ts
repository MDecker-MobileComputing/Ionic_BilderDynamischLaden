import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eldecker.ionic.bilderdynamisch',
  appName: 'BilderDynamischLaden',
  webDir: 'www'
};

export default config;
