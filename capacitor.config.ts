import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.eldecker.ionic.bilderdynamisch',
  appName: 'Flickr-Suche',
  webDir: 'www'
};

export default config;
